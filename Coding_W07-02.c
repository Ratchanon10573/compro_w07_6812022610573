#include <stdio.h>

int main() {
    int x, y;
    // รับค่าจากผู้ใช้
    printf("Enter the value of x: ");
    scanf("%d", &x);

    printf("Enter the value of y: ");
    scanf("%d", &y);

    if (x > y) { // ถ้า x มากกว่า y
        printf("x is greater than y\n");
    } else if (x < y) { // ถ้า x น้อยกว่า y
        printf("x is less than y\n");
    } else if (x == y) { // ถ้า x เท่ากับ y
        printf("x is equal to y\n");
    }

    return 0;
}
/* อธิบายการทำงานของโปรแกรม
1. โปรแกรมเริ่มต้นด้วยการรวมไลบรารีมาตรฐาน <stdio.h> เพื่อใช้ฟังก์ชันการป้อนข้อมูลและแสดงผล
2. ในฟังก์ชัน main() โปรแกรมประกาศตัวแปรจำนวนเต็มสองตัวคือ x และ y
3. โปรแกรมแสดงข้อความเพื่อขอให้ผู้ใช้ป้อนค่าของ x และ y
4. โปรแกรมใช้ฟังก์ชัน scanf() เพื่ออ่านค่าที่ผู้ใช้ป้อนและเก็บไว้ในตัวแปร x และ y
5. โปรแกรมใช้คำสั่ง if-else if เพื่อตรวจสอบความสัมพันธ์ระหว่าง x และ y:
   - ถ้า x มากกว่า y โปรแกรมจะแสดงข้อความ "x is greater than y"
   - ถ้า x น้อยกว่า y โปรแกรมจะแสดงข้อความ "x is less than y"
   - ถ้า x เท่ากับ y โปรแกรมจะแสดงข้อความ "x is equal to y"
6. โปรแกรมจบการทำงานและคืนค่า 0 เพื่อแสดงว่าการทำงานเสร็จสมบูรณ์
*/